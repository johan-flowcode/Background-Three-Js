{
  "name": "background-three-js",
  "version": "1.0.0",
  "description": "",
 
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx parcel index.html --open",
    "build": "npx parcel build index.html",
    "clean": "npx parcel cache clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "css": "^3.0.0",
    "three": "^0.161.0"
  },
  "devDependencies": {
    "parcel": "^2.11.0"
  }
}
